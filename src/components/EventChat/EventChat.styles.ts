import { createStyles, makeStyles } from '@material-ui/styles';

export const eventChatStyles = makeStyles(() => {
  return createStyles({
    gridContainer: {
      width: '100%',
    },
    gridItem: {
      padding: '8px',
    },
  });
});
